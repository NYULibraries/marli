language: ruby

before_script:
  - mysql -e 'create database marli_test;'
  - bundle exec rake db:schema:load
  - bundle exec rake db:fixtures:load
  - bundle exec rake tmp:cache:clear

rvm:
  - "1.9.3"
  - "2.0.0"

git:
  submodules: false

notifications:
  email:
    - barnaby.alter@nyu.edu
