<%= paginate @users %>

<p><%= page_entries_info @users %></p>

<%= paginate @users %>

<div class="btn-group">
  <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
    Action
    <span class="caret"></span>
  </a>
  <ul class="dropdown-menu">
    <li><%= link_to new_user_url do %><i class="icon-user"></i> Add user<% end -%></li>
    <li><%= link_to users_url(:format => :csv) do %><i class="icon-download-alt"></i> Download as CSV<% end -%></li>
  </ul>
</div>

<div class="btn-group">
  <a class="btn btn-danger dropdown-toggle" data-toggle="dropdown" href="#">
    Delete
    <span class="caret"></span>
  </a>
  <ul class="dropdown-menu">
    <li><%= link_to reset_submissions_url, :data => {:confirm => 'This will reset all user submissions but not delete the users. Are you sure?'} do %><i class="icon-refresh"></i> Reset submissions<% end -%></li>
    <li><%= link_to clear_patron_data_url, :data => {:confirm => 'This will delete all non-admin patrons and cannot be undone. Are you sure?'} do %><i class="icon-ban-circle"></i> Clear all user data<% end -%></li>
  </ul>
</div>

<br /><br />

<table class="table table-bordered">
  <thead>
    <tr>
  		<th><%= sortable "username"%></th>
  		<th><%= sortable "firstname"%></th>
  		<th><%= sortable "lastname"%></th>
  		<th><%= sortable "email"%></th>
  		<th>Department</th>
  		<th>School</th>
  		<th><%= sortable "submitted_request", "Registered"%></th>
  		<th>Status</th>
  		<th colspan="4">Actions</th>
    </tr>
  </thead>
  <tbody>
   <% @users.each do |user| %>
   <% h = user.user_attributes %>
  	<tr <%= (user_submitted(user)) ? "class=success" : ''%>>
    	<td><%=h user.username %></td>
    	<td><%=h user.firstname %></td>
    	<td><%=h user.lastname %></td>
    	<td><%=h word_break user.email %></td>
    	<td><%=h h[:department]%></td>
  	  <td><%=h h[:school]%></td>
  	  <td><%= (user_submitted(user)) ? "<i class=\"icon-ok\"></i>".html_safe : ''%></td>
    	<td><%=h user_status(user) %></td>
    	<td><%= link_to 'Show', user %></td>
    	<td><%= link_to 'Reset', reset_submissions_url, :id => user.id, :data => {:confirm => 'This will removed the submitted status from this user. Are you sure?'} %></td>
    	<td><%= link_to_if(user.username != current_user.username, 'Destroy', user, :data => {:confirm => 'Are you sure?'}, :method => :delete) %></td>
 	  </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @users %>




